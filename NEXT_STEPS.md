# Следующие шаги

## ✅ Текущий статус

- ✅ Все ключи настроены (6/6)
- ✅ Бот запущен и работает
- ✅ Все функции реализованы
- ✅ Защита от множественных запусков добавлена

## 🧪 Этап 1: Тестирование базовых команд

### В Telegram-чате `-5010232164`:

1. **Проверка статуса:**
   ```
   /eventstatus
   ```
   Ожидается: статус "Не активен", результат: 0

2. **Запуск события (от администратора):**
   ```
   /pumpOrDump
   ```
   Ожидается: "Ивент запущен! Текущий результат: 0"

3. **Тестирование голосования:**
   ```
   /pump
   /pump
   /dump
   /pump
   /eventstatus
   ```
   Проверьте, что счетчик меняется корректно

4. **Тестирование завершения PUMP:**
   ```
   /pumpOrDump  (запустить новое событие)
   /pump
   /pump
   /pump
   /pump
   /pump  (5-й раз должен завершить событие)
   ```
   Ожидается:
   - Сообщение "LEEEEET'S PUMP TONDEV❗️❗️" (10 раз)
   - Попытка автоматической покупки TONDEV

5. **Тестирование завершения DUMP:**
   ```
   /pumpOrDump  (запустить новое событие)
   /dump
   /dump
   /dump
   /dump
   /dump  (5-й раз должен завершить событие)
   ```
   Ожидается:
   - Сообщение "LEEEEET'S DUMP TONDEV❗️❗️" (10 раз)
   - Попытка автоматической продажи всех TONDEV токенов

## 🔍 Этап 2: Проверка логов

Во время тестирования следите за логами:

```bash
tail -f bot_test.log
```

**Что проверять:**
- ✅ Команды обрабатываются корректно
- ✅ Счетчик меняется правильно
- ✅ События завершаются при достижении порога
- ✅ Автоматическая покупка/продажа выполняется (если настроен кошелек)
- ❌ Нет ошибок в логах

## 💰 Этап 3: Тестирование с реальными транзакциями

⚠️ **ВНИМАНИЕ**: Это требует реальных средств!

### Перед тестированием:

1. **Проверьте баланс кошелька:**
   - Для покупки: баланс >= `BUY_AMOUNT_TON + 1 TON` (резерв)
   - Для продажи: наличие TONDEV токенов на кошельке

2. **Убедитесь, что кошелек активен:**
   - Кошелек должен быть инициализирован в блокчейне
   - Проверьте на https://tonviewer.com

3. **Начните с малых сумм:**
   - Установите `BUY_AMOUNT_TON=0.1` для тестов
   - Проверьте работу на небольшой сумме

### Тестирование покупки:

1. Запустите событие `/pumpOrDump`
2. Доведите счетчик до +5 через `/pump`
3. Проверьте:
   - Транзакция отправлена в блокчейн
   - Уведомление в чат с деталями
   - TONDEV токены получены на кошелек

### Тестирование продажи:

1. Убедитесь, что есть TONDEV токены на кошельке
2. Запустите событие `/pumpOrDump`
3. Доведите счетчик до -5 через `/dump`
4. Проверьте:
   - Транзакция отправлена в блокчейн
   - Уведомление в чат с количеством проданных токенов
   - TON получены на кошелек

## ⚙️ Этап 4: Настройка для продакшена

### Изменить порог завершения:

В `bot.js` измените:
```javascript
const FINISH_THRESHOLD = 500; // Было 5 для тестов
```

### Проверка безопасности:

- [ ] Убедитесь, что `secretkeys.env` добавлен в `.gitignore`
- [ ] Проверьте, что ключи не коммитятся в git
- [ ] Убедитесь, что кошелек имеет достаточный баланс
- [ ] Проверьте права администратора в чате

## 📊 Этап 5: Мониторинг

### Что отслеживать:

1. **Логи бота:**
   ```bash
   tail -f bot_test.log
   ```

2. **Состояние бота:**
   ```bash
   cat bot_state.json
   ```

3. **Процессы:**
   ```bash
   ps aux | grep "node bot.js"
   ```

4. **Транзакции в блокчейне:**
   - Проверяйте транзакции на https://tonviewer.com
   - Отслеживайте баланс кошелька

## 🚀 Готово к использованию!

После успешного тестирования бот готов к работе в продакшене.

### Быстрая команда для запуска:

```bash
# Остановить все экземпляры и запустить новый
pkill -f "node bot.js" && npm start
```

### Команды для проверки:

```bash
# Проверить статус бота
ps aux | grep "node bot.js" | grep -v grep

# Посмотреть логи
tail -f bot_test.log

# Проверить состояние
cat bot_state.json
```

## 📝 Чеклист перед продакшеном

- [ ] Протестированы все команды
- [ ] Протестирована автоматическая покупка
- [ ] Протестирована автоматическая продажа
- [ ] Порог изменен на 500 (для продакшена)
- [ ] Кошелек имеет достаточный баланс
- [ ] Все ключи настроены и безопасны
- [ ] Бот работает стабильно без ошибок
- [ ] Логи проверены на наличие ошибок




