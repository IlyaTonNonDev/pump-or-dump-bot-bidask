# Чеклист для проверки бота на ботхосте

## 1. Проверка кода
Убедитесь, что на ботхосте актуальный код с исправлением покупок:
```bash
# На ботхосте выполните:
cd /путь/к/боту
git pull origin main
# Проверьте, что файл bot.js содержит строку:
grep -n "Для покупки сразу возвращаем результат" bot.js
# Должна быть строка около 286
```

## 2. Проверка токена бота
Убедитесь, что используется правильный токен:
```bash
# Проверьте переменные окружения на ботхосте
# Токен должен начинаться с того же числа, что и в secretkeys.env
```

## 3. Перезапуск бота
**ВАЖНО:** После обновления кода бот нужно перезапустить:
```bash
# Остановите старый процесс
pkill -f "node bot.js"
# Или через панель управления ботхоста

# Запустите заново
npm start
```

## 4. Проверка активации в группе
Убедитесь, что бот активирован в группе:
- Отправьте `/start` боту в группе
- Проверьте логи - должно быть: `[/START] ✅ Group chat [chatId] added to notification list`

## 5. Проверка логов
Проверьте логи на наличие ошибок:
```bash
# Если логи в файле:
tail -f bot.log

# Или в панели управления ботхоста
```

**Что искать в логах:**
- `[MONITOR] ⚠️ No notification chats registered` - бот не активирован
- `[MONITOR] 💬 Processing chat [chatId]` - бот работает
- `[2. PARSE_TRANSACTION] ✅ Parsed successfully` - транзакция обработана
- `[4. SEND_NOTIFICATION] ✅ Notification sent` - уведомление отправлено
- Ошибки с префиксом `❌`

## 6. Проверка переменных окружения
Убедитесь, что на ботхосте установлены:
- `TELEGRAM_BOT_TOKEN` - токен правильного бота
- `TON_API_KEY` - ключ TON API

## 7. Проверка конфликтов
Если видите ошибку `409 Conflict`, значит запущено несколько экземпляров:
```bash
# Остановите все процессы
pkill -f "node bot.js"
# Запустите один
npm start
```

## Быстрая диагностика
Если ничего не помогает, проверьте:
1. Какой токен бота используется на ботхосте?
2. Есть ли логи об активации группы (`/start`)?
3. Есть ли ошибки в логах?
4. Перезапускали ли бота после обновления кода?

